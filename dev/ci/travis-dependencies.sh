# weathermap dependencies
pecl install gd
pecl install mysql
pecl install xdebug
pecl install snmp

# Cacti dependencies
pecl install mbstring
pecl install curl
pecl install ldap
pecl install gmp
pecl install xml
pecl install gettext
pecl install sqlite3
pecl install ast


