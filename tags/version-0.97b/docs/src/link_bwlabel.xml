<?xml version="1.0" encoding="UTF-8"?>
<configentry>
    <keyword>BWLABEL</keyword>

    <scope>LINK</scope>

    <anchor>LINK_BWLABEL</anchor>

    <definition>BWLABEL

        <meta>formatname</meta>
    </definition>

    <description xmlns="http://www.w3.org/1999/xhtml">
        <p>Specifies the type of 'bandwidth' label shown on each link.</p>

        <p>The default is 'percent', but you can also have 'none', 'unformatted' or
        'bits'.
        'bits' shows the actual bandwidth, formatted using K,M,T,G suffixes where
        appropriate. 'unformatted' takes the value from the TARGET and displays it
        without any formatting
        - this can be useful for mapping things other than bandwidth. 'none' hides the
        bandwidth label altogether.
        </p>
    </description>

    <changes>
        <change version="0.8">Added unformatted format.
        </change>

        <change version="0.7">Changed from global BWLABELS to per-link BWLABEL.
        </change>
    </changes>
</configentry>