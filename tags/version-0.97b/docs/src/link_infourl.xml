<?xml version="1.0" encoding="UTF-8"?>
<configentry>
    <keyword>INFOURL</keyword>

    <scope>LINK</scope>

    <anchor>LINK_INFOURL</anchor>

    <definition>INFOURL

        <meta>url</meta>
    </definition>

    <description xmlns="http://www.w3.org/1999/xhtml">
        <p>Creates a hyperlink in the HTML output.</p>

        <p>If you are using the HTML output facility, then a link is added to the
        &lt;map> section of the HTML so that when you click on the (weathermap) link,
        you are taken to the url specified here.</p>

        <p>It is also possible to specify the INFOURL for the 'in' and 'out' halves of a
        link individually, using ININFOURLand OUTINFOURL.</p>
    </description>

    <changes>
        <change version="0.95">Added IN/OUT support
        </change>
    </changes>
</configentry>