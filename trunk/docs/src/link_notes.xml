<?xml version="1.0" encoding="UTF-8"?>
<configentry>
    <keyword>NOTES</keyword>

    <scope>LINK</scope>

    <anchor>LINK_NOTES</anchor>

    <definition>NOTES

        <meta>notes text</meta>
    </definition>

    <description xmlns="http://www.w3.org/1999/xhtml">
        <p>Specifies the text or HTML notes for a link. </p>

        <p>The
        <em>notes text</em> appears in a popup box when the user hovers their mouse over
        the link. If an OVERLIBGRAPH is specified too, then the text appears below the
        graph.</p>

        <p>The map HTMLSTYLE must be set to 'overlib' to enable any of the mouse-hover
        functionality.</p>

        <p>It is also possible to specify the NOTES for the 'in' and 'out' halves of a
        link individually, using INNOTES and OUTNOTES.</p>
    </description>

    <changes>
        <change version="0.95">Added IN/OUT support
        </change>

        <change version="0.9">Originally added NOTES.
        </change>
    </changes>
</configentry>
