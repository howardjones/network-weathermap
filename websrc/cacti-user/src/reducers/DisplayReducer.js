
const INITIAL_STATE = {
    cycling: false,
    cycle_group: 0,
    cycle_controls_visible: false,
    fullscreen: false
};

export default function (state = INITIAL_STATE, action) {

    // No actions yet!
    return state;
};